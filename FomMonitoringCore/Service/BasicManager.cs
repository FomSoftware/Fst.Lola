using FomMonitoringCore.Model;

namespace FomMonitoringCore.Service
{
    public class BasicManager
    {
        public static bool ValidateCredentials(LoginModel login)
        {
            bool result = true;
            return result;
        }
    }
}